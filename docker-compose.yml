version: '3'

services:
  # db:
  #   image: postgres
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432"
  # redis:
  #   image: redis:latest
  #   volumes:
  #     - redis_data:/data
  #   ports:
  #     - 6379:6379

  nats:
    image: nats:latest
    ports:
      - "4222:4222"
      - "6222:6222"
      - "8222:8222"

  vault:
    image: vault:latest
    ports:
      - "8200:8200"

# volumes:
  # db_data:
  # redis_data:
